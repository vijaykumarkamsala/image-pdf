[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "ipw-licence-registry"
version = "0.1.0"
description = "Production-safe licence and release gate evaluation."
requires-python = ">=3.11"
license = "MIT"
dependencies = [
  "ipw-contracts==0.1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["ipw*"]
namespaces = true

[tool.setuptools.package-data]
"ipw.licence_registry" = ["py.typed"]
