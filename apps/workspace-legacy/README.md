# Frozen Legacy Workspace UI

This directory preserves the pre-V2 vanilla JavaScript workspace as legacy
evidence. It is not the production React customer application.

Do not redesign, repair or incrementally convert this UI to React. Keep behavior
changes out of this directory unless a future task explicitly authorizes a
legacy preservation fix.

The local legacy server remains:

```powershell
python tools/serve_workspace.py
```

